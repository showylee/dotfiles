var userdir = require('..');
var chai = require('chai');
var assert = chai.assert;

describe('userdir', function() {
  it('should be truthy', function() {
    assert.ok(userdir, 'userdir is truthy');
  });
});